type User {
  id: ID!
  username: String!
  created: String!
  bookmarks: [Idea!]
  ideas: [Idea!]
  comments: [Comment!]
}
extend type Query {
  users(page: Int): [User!]
  user(username: String!): User
}